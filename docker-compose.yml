services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: pauseai
      POSTGRES_PASSWORD: pauseai
      POSTGRES_DB: pauseai
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
